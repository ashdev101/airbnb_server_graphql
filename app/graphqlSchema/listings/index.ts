

import { types } from './type';
import { queries } from './query';
import { mutations } from './mutations';
import { resolvers } from './resolvers';

export const Listing = { types, queries, mutations, resolvers };
